# Visual Producer — prompt editing

Il prompt operativo per il ritocco post-selezione è in:

`config/brand/image_edit_task_prompt.md`

Configurabile via `VISUAL_PRODUCER_PROMPT_PATH` nel `.env`.

Il template `/produce` in `story_agent.md` resta per altri flussi (copy pack, riferimento GPT).
